#生成200个16位激活码

import random,string

#生成字符串函数
def genRandomString(slen=5):
    return ''.join(random.sample(string.ascii_letters+string.digits,slen))

i=0
while(i<200):
    code=genRandomString(slen=20)
    print(code)
    i+=1
